# 🏢 Data Warehouses
# Kho Dữ liệu (OLAP)

This folder is dedicated to the OLAP (Online Analytical Processing) architectures discussed in Stage 6.
*Thư mục này dành riêng cho các kiến trúc OLAP được thảo luận ở Giai đoạn 6.*

## Content
- **Star Schema Models:** See `docs/00-architecture/02_olap_star_schema.sql` for the core table definitions.
- **Materialized Views:** See `docs/06-warehousing/01_materialized_view_refresh.sql` for advanced query rewrite setups.
- **Partitioning:** See `docs/00-architecture/03_partitioned_table.sql`.

*To initialize the Data Warehouse, execute the scripts located in the `docs` directory as referenced above.*
*(Để khởi tạo Kho dữ liệu, hãy chạy các tập lệnh nằm trong thư mục `docs` như đã tham chiếu ở trên).*